<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Richasy.WinUIKernel.Share.Converters"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:local="using:Richasy.MpvKernel.WinUI.Controls">
    <ResourceDictionary.MergedDictionaries>
        <XamlControlsResources />
    </ResourceDictionary.MergedDictionaries>

    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityReverseConverter" IsReverse="True" />
    <converters:ObjectToBoolConverter x:Key="ObjectToBoolConverter" />
    <converters:ObjectToBoolConverter x:Key="ObjectToBoolReverseConverter" IsReverse="True" />
    <converters:ObjectToVisibilityConverter x:Key="ObjectToVisibilityConverter" />
    <converters:ObjectToVisibilityConverter x:Key="ObjectToVisibilityReverseConverter" IsReverse="True" />

    <Style x:Key="BasicPlayerIconButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="40" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Height" Value="40" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style x:Key="PrimaryPlayerButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="52" />
        <Setter Property="Height" Value="52" />
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0.0" Color="{ThemeResource SolidBackgroundFillColorQuarternary}" />
                    <GradientStop Offset="1.0" Color="{ThemeResource SolidBackgroundFillColorBaseAlt}" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
        <Setter Property="Foreground" Value="{ThemeResource ButtonForeground}" />
        <Setter Property="BorderBrush">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0.0" Color="{ThemeResource SystemAccentColorLight1}" />
                    <GradientStop Offset="1.0" Color="{ThemeResource SystemAccentColorDark2}" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderThickness" Value="4" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="CornerRadius" Value="26" />
        <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
        <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin" Value="-3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <ContentPresenter
                        x:Name="ContentPresenter"
                        Padding="{TemplateBinding Padding}"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        AnimatedIcon.State="Normal"
                        AutomationProperties.AccessibilityView="Raw"
                        Background="{TemplateBinding Background}"
                        BackgroundSizing="{TemplateBinding BackgroundSizing}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Content="{TemplateBinding Content}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        ContentTransitions="{TemplateBinding ContentTransitions}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Foreground="{TemplateBinding Foreground}">
                        <ContentPresenter.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </ContentPresenter.BackgroundTransition>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Offset="0.0" Color="{ThemeResource ControlSolidFillColorDefault}" />
                                                        <GradientStop Offset="1.0" Color="{ThemeResource SolidBackgroundFillColorBaseAlt}" />
                                                    </LinearGradientBrush>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0.7,0.9">
                                                        <GradientStop Offset="0.0" Color="{ThemeResource SystemAccentColorLight2}" />
                                                        <GradientStop Offset="1.0" Color="{ThemeResource SystemAccentColorDark1}" />
                                                    </LinearGradientBrush>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ButtonForegroundPointerOver}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                    <VisualState.Setters>
                                        <Setter Target="ContentPresenter.(AnimatedIcon.State)" Value="PointerOver" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Offset="0.0" Color="{ThemeResource ControlSolidFillColorDefault}" />
                                                        <GradientStop Offset="1.0" Color="{ThemeResource SolidBackgroundFillColorBaseAlt}" />
                                                    </LinearGradientBrush>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0.9,1">
                                                        <GradientStop Offset="0.0" Color="{ThemeResource SystemAccentColorLight1}" />
                                                        <GradientStop Offset="1.0" Color="{ThemeResource SystemAccentColorDark2}" />
                                                    </LinearGradientBrush>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ButtonForegroundPressed}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                    <VisualState.Setters>
                                        <Setter Target="ContentPresenter.(AnimatedIcon.State)" Value="Pressed" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ButtonBackgroundDisabled}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ButtonBorderBrushDisabled}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource ButtonForegroundDisabled}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                    <VisualState.Setters>
                                        <!--  DisabledVisual Should be handled by the control, not the animated icon.  -->
                                        <Setter Target="ContentPresenter.(AnimatedIcon.State)" Value="Normal" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </ContentPresenter>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Thickness x:Key="MpvTransportControlContainerPadding">16,0</Thickness>

    <Style BasedOn="{StaticResource DefaultMpvTransportControlsStyle}" TargetType="local:MpvTransportControls" />

    <Style x:Key="DefaultMpvTransportControlsStyle" TargetType="local:MpvTransportControls">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:MpvTransportControls">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid Padding="{StaticResource MpvTransportControlContainerPadding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="LeftColumn" />
                                <ColumnDefinition x:Name="MiddleColumn" Width="Auto" />
                                <ColumnDefinition x:Name="RightColumn" />
                            </Grid.ColumnDefinitions>
                            <!--  左侧自定义元素  -->
                            <ContentPresenter
                                x:Name="InfoPresenter"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding InfoElement}" />
                            <!--  核心播放控制  -->
                            <StackPanel
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <Button
                                    x:Name="PrevButton"
                                    Margin="0,0,8,0"
                                    IsEnabled="{TemplateBinding IsPreviousButtonEnabled}"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}"
                                    ToolTipService.ToolTip="{TemplateBinding PreviousButtonToolTip}"
                                    Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsVideoNavigationButtonsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ic:SymbolIcon
                                        FontSize="16"
                                        IconVariant="Filled"
                                        Symbol="Previous" />
                                </Button>
                                <Button
                                    x:Name="BackwardButton"
                                    Margin="0,0,8,0"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}"
                                    Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsSkipButtonsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ic:SymbolIcon
                                        FontSize="16"
                                        IconVariant="Filled"
                                        Symbol="SkipBack10" />
                                </Button>
                                <Grid>
                                    <Button
                                        x:Name="PlayPauseButton"
                                        IsEnabled="{TemplateBinding IsPlayPauseButtonEnabled}"
                                        Style="{StaticResource PrimaryPlayerButtonStyle}">
                                        <ic:SymbolIcon
                                            FontSize="22"
                                            IconVariant="Filled"
                                            Symbol="{TemplateBinding PlayPauseSymbol}" />
                                    </Button>
                                    <ProgressRing
                                        Width="52"
                                        Height="52"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        BorderThickness="4"
                                        IsActive="{TemplateBinding IsBuffering}" />
                                </Grid>
                                <Button
                                    x:Name="ForwardButton"
                                    Margin="8,0,0,0"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}"
                                    Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsSkipButtonsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ic:SymbolIcon
                                        FontSize="16"
                                        IconVariant="Filled"
                                        Symbol="SkipForward30" />
                                </Button>
                                <Button
                                    x:Name="NextButton"
                                    Margin="8,0,0,0"
                                    IsEnabled="{TemplateBinding IsNextButtonEnabled}"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}"
                                    ToolTipService.ToolTip="{TemplateBinding NextButtonToolTip}"
                                    Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsVideoNavigationButtonsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <ic:SymbolIcon
                                        FontSize="16"
                                        IconVariant="Filled"
                                        Symbol="Next" />
                                </Button>
                            </StackPanel>

                            <!--  右侧附加控制  -->
                            <StackPanel
                                Grid.Column="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <StackPanel
                                    x:Name="FullVolumeContainer"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal"
                                    Spacing="8">
                                    <ic:SymbolIcon
                                        x:Name="FullVolumeIcon"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Symbol="Speaker2" />
                                    <Slider
                                        x:Name="FullVolumeSlider"
                                        Width="120"
                                        VerticalAlignment="Center"
                                        LargeChange="10"
                                        Maximum="100"
                                        Minimum="0"
                                        SmallChange="1"
                                        TickFrequency="1"
                                        Value="{TemplateBinding Volume}" />
                                </StackPanel>
                                <Button
                                    x:Name="SlimVolumeButton"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}"
                                    Visibility="Collapsed">
                                    <ic:SymbolIcon
                                        x:Name="SlimVolumeIcon"
                                        FontSize="14"
                                        Symbol="Speaker2" />
                                    <Button.Flyout>
                                        <Flyout Placement="Top">
                                            <Slider
                                                x:Name="SlimVolumeSlider"
                                                Width="200"
                                                VerticalAlignment="Center"
                                                LargeChange="10"
                                                Maximum="100"
                                                Minimum="0"
                                                SmallChange="1"
                                                TickFrequency="1"
                                                Value="{TemplateBinding Volume}" />
                                        </Flyout>
                                    </Button.Flyout>
                                </Button>
                                <Button
                                    x:Name="SpeedButton"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}">
                                    <TextBlock x:Name="SpeedBlock" FontSize="14" />
                                    <Button.Flyout>
                                        <Flyout Placement="Top">
                                            <Slider
                                                x:Name="SpeedSlider"
                                                Width="200"
                                                VerticalAlignment="Center"
                                                LargeChange="0.25"
                                                Maximum="3"
                                                Minimum="0.1"
                                                SmallChange="0.25"
                                                StepFrequency="0.25"
                                                TickFrequency="0.25"
                                                TickPlacement="Outside"
                                                Value="{TemplateBinding Speed}" />
                                        </Flyout>
                                    </Button.Flyout>
                                </Button>
                                <Button
                                    x:Name="CompactOverlayButton"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}">
                                    <ic:SymbolIcon
                                        x:Name="CompactOverlayIcon"
                                        FontSize="14"
                                        Symbol="ContractUpRight" />
                                </Button>
                                <Button
                                    x:Name="FullScreenButton"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource BasicPlayerIconButtonStyle}">
                                    <ic:SymbolIcon
                                        x:Name="FullScreenIcon"
                                        FontSize="14"
                                        Symbol="FullScreenMaximize" />
                                </Button>
                            </StackPanel>
                        </Grid>

                        <!--  底部进度条  -->
                        <Grid
                            Grid.Row="1"
                            Margin="16,0,16,20"
                            ColumnSpacing="12"
                            Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                x:Name="ProgressBlock"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPreviewProgressVisible, Converter={StaticResource BoolToVisibilityReverseConverter}}" />
                            <TextBlock
                                x:Name="PreviewProgressBlock"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPreviewProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                            <Slider
                                x:Name="ProgressSlider"
                                Grid.Column="1"
                                Maximum="{TemplateBinding Duration}"
                                Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPreviewProgressVisible, Converter={StaticResource BoolToVisibilityReverseConverter}}"
                                Value="{TemplateBinding Progress}" />
                            <Slider
                                x:Name="PreviewProgressSlider"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Maximum="{TemplateBinding Duration}"
                                Visibility="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsPreviewProgressVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                Value="{TemplateBinding PreviewProgress}" />
                            <TextBlock
                                x:Name="DurationBlock"
                                Grid.Column="2"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}" />
                        </Grid>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualState x:Name="WideState">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="1000" />
                                    </VisualState.StateTriggers>
                                </VisualState>
                                <VisualState x:Name="BelowWideState">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="700" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="FullVolumeContainer.Visibility" Value="Collapsed" />
                                        <Setter Target="SlimVolumeButton.Visibility" Value="Visible" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="MediumState">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="500" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="FullVolumeContainer.Visibility" Value="Collapsed" />
                                        <Setter Target="SlimVolumeButton.Visibility" Value="Visible" />
                                        <Setter Target="BackwardButton.Visibility" Value="Collapsed" />
                                        <Setter Target="ForwardButton.Visibility" Value="Collapsed" />
                                        <Setter Target="PrevButton.Visibility" Value="Collapsed" />
                                        <Setter Target="NextButton.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="NarrowState">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="0" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="FullVolumeContainer.Visibility" Value="Collapsed" />
                                        <Setter Target="SlimVolumeButton.Visibility" Value="Visible" />
                                        <Setter Target="BackwardButton.Visibility" Value="Collapsed" />
                                        <Setter Target="ForwardButton.Visibility" Value="Visible" />
                                        <Setter Target="PrevButton.Visibility" Value="Collapsed" />
                                        <Setter Target="NextButton.Visibility" Value="Visible" />
                                        <Setter Target="LeftPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="LeftColumn.Width" Value="Auto" />
                                        <Setter Target="RootGrid.Height" Value="120" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
